/*
    Para realizar o exercício, crie uma página HTMLsimples e um arquivo JS como foi feito nas aulas.
    Dentro do arquivo JS escreva o código para chegar noresultado final.

    Objetivo: apresente a mensagem, "Seja bem vindo(a),Fulano!"

    1- Peça que o usuário informe o seu nome usando o prompt(lembre-se da aula de variáveis);

    2- Apresente a mensagem de boas vindas seguindo donome que a pessoa digitou no passo 1.

    Obs: pra apresentar a mensagem de boas vindas pode usaro alert também.  
*/

const nomeDoUsuario = prompt("Qual é o seu nome?")

console.log("Seja bem vindo(a), " + nomeDoUsuario + "!")